export default function AccentTitle ({text}) {
  return (
    // select-none
    <div className={`inline-flex text-white font-semibold uppercase tracking-max text-sm lg:text-base mb-4`}>
      {text}
    </div>)
}
