export default function AccentTitle ({text}) {
  return (
    // select-none
    <div className={`text-white font-semibold uppercase tracking-max text-xs lg:text-sm' mb-4`}>
      {text}
    </div>)
}
